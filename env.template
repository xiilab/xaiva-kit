# =============================================================================
# XaivaKit - Environment Variables Template
# =============================================================================
# 
# 사용 방법:
# 1. 이 파일을 복사하여 .env 파일을 생성합니다: cp env.template .env
# 2. 아래 변수들의 값을 실제 환경에 맞게 설정합니다.
# 3. .env 파일은 절대 Git에 커밋하지 마세요 (.gitignore에 포함됨)
#
# =============================================================================

# -----------------------------------------------------------------------------
# GitHub Access (Optional - 추후 Xaiva Media 소스 관리 시 사용 예정)
# -----------------------------------------------------------------------------
# GitHub Personal Access Token (repo 권한 필요)
# 현재는 직접 클론하지 않으므로 선택 사항이나, 추후 확장을 위해 준비
GITHUB_TOKEN=ghp_your_token_here

# -----------------------------------------------------------------------------
# Docker Registry (Optional - 추후 확장 예정)
# -----------------------------------------------------------------------------
# Private Docker Registry 정보 (필요 시 사용)
# DOCKER_REGISTRY_URL=registry.example.com
# DOCKER_REGISTRY_USERNAME=your_username
# DOCKER_REGISTRY_PASSWORD=your_password

# -----------------------------------------------------------------------------
# Build Configuration (Optional)
# -----------------------------------------------------------------------------
# 빌드 시 기본 프리셋 (build.py에서 --preset 플래그로 오버라이드 가능)
# DEFAULT_PRESET=ubuntu22.04-cuda11.8-torch2.1

# Xaiva Media 소스 경로 (로컬 경로 또는 Git 서브트리 경로)
# XAIVA_MEDIA_SOURCE_PATH=/path/to/xaiva-media-source

# -----------------------------------------------------------------------------
# Timezone and Locale
# -----------------------------------------------------------------------------
TZ=Asia/Seoul
LC_ALL=C.UTF-8

